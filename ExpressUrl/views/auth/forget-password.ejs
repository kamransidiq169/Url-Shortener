<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password | MyApp</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/material-design-iconic-font@2.2.0/dist/css/material-design-iconic-font.min.css" />
  <style>
    * {
      margin: 0; padding: 0; box-sizing: border-box;
    }

    body {
      font-family: "Inter", sans-serif;
      background: radial-gradient(circle at 25% 25%, #eef2ff, #e0e7ff, #f5f3ff);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      color: #1e293b;
    }

    /* Card Container */
    .card {
      width: 100%;
      max-width: 460px;
      background: rgba(255, 255, 255, 0.75);
      border-radius: 20px;
      backdrop-filter: blur(25px);
      border: 1px solid rgba(255, 255, 255, 0.25);
      box-shadow: 0 8px 35px rgba(99, 102, 241, 0.15);
      position: relative;
      overflow: hidden;
      transition: all 0.3s ease;
      padding: 42px 36px;
      animation: fadeIn 0.6s ease-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .card::before {
      content: "";
      position: absolute;
      top: -25%;
      left: -25%;
      width: 150%;
      height: 150%;
      background: conic-gradient(from 90deg at 50% 50%, #818cf8, #a78bfa, #c084fc, #818cf8);
      filter: blur(60px);
      opacity: 0.15;
      z-index: 0;
      animation: rotate 12s linear infinite;
    }

    @keyframes rotate {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .card-content {
      position: relative;
      z-index: 2;
    }

    /* Status Badge */
    .status {
      text-align: center;
      margin-bottom: 20px;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 18px;
      border-radius: 12px;
      font-weight: 500;
      font-size: 0.95rem;
    }

    .badge.success {
      background: rgba(16, 185, 129, 0.15);
      color: #047857;
    }

    .badge.warning {
      background: rgba(251, 191, 36, 0.15);
      color: #b45309;
    }

    /* Title */
    .title {
      text-align: center;
      font-size: 1.8rem;
      font-weight: 700;
      background: linear-gradient(90deg, #6366f1, #8b5cf6);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 30px;
    }

    /* Input Field */
    .input-wrapper {
      position: relative;
      display: flex;
      align-items: center;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.35);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.4);
      margin-bottom: 20px;
      transition: all 0.3s ease;
    }

    .input-wrapper:focus-within {
      border-color: rgba(129, 140, 248, 0.7);
      box-shadow: 0 0 15px rgba(129, 140, 248, 0.3);
      background: rgba(255, 255, 255, 0.55);
    }

    .input-wrapper i {
      position: absolute;
      left: 14px;
      color: #6366f1;
      font-size: 1.2rem;
    }

    .input-wrapper input {
      width: 100%;
      padding: 14px 14px 14px 44px;
      border: none;
      background: transparent;
      font-size: 1rem;
      color: #1e293b;
      font-weight: 500;
    }

    .input-wrapper input::placeholder {
      color: rgba(71, 85, 105, 0.6);
    }

    .input-wrapper input:focus {
      outline: none;
    }

    /* Button */
    .btn {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 12px;
      background: linear-gradient(90deg, #6366f1, #8b5cf6);
      color: white;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
      letter-spacing: 0.3px;
    }

    .btn:hover {
      background: linear-gradient(90deg, #4f46e5, #7c3aed);
      box-shadow: 0 6px 18px rgba(99, 102, 241, 0.25);
      transform: translateY(-1px);
    }

    /* Error Message */
    .flash-error {
      background: rgba(254, 202, 202, 0.5);
      color: #b91c1c;
      padding: 10px;
      border-radius: 10px;
      text-align: center;
      font-size: 0.9rem;
      margin-bottom: 12px;
    }

    /* Responsive */
    @media (max-width: 480px) {
      .card {
        padding: 32px 24px;
      }
      .title {
        font-size: 1.6rem;
      }
    }
  </style>
</head>
<body>

  <div class="card">
    <div class="card-content">
      <div class="status">
        <% if (user) { %>
          <div class="badge success">
            <i class="zmdi zmdi-check-circle"></i>
            <span>You are logged in</span>
          </div>
        <% } else { %>
          <div class="badge warning">
            <i class="zmdi zmdi-alert-circle"></i>
            <span>You are not logged in</span>
          </div>
        <% } %>
      </div>

      <% if (formSubmitted) { %>
        <p style="text-align:center; font-size:1rem; line-height:1.6; color:#334155;">
          If the email you entered is correct, we have sent you a link to reset your password.
        </p>
      <% } else { %>

      <h1 class="title">Forgot Password</h1>

      <form action="/forget-password" method="post">
        <div class="input-wrapper">
          <i class="zmdi zmdi-email"></i>
          <input type="email" name="email" id="email" placeholder="Enter your registered email" required />
        </div>

        <% if (errors && errors.length > 0) { %>
          <% errors.forEach(error => { %>
            <p class="flash-error"><%= error %></p>
          <% }) %>
        <% } %>

        <button type="submit" class="btn">Submit</button>
      </form>

      <% } %>
    </div>
  </div>

</body>
</html>
