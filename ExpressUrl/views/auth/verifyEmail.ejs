// ...existing code...
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify Email</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <%- include('../navbar') %>

    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card mt-4">
          <div class="card-body text-center">
            <h3 class="mb-2">Email Verification</h3>
            <p class="text-muted mb-4">A verification link or code was sent to your email. Enter the 8‑digit code below to verify your account.</p>

      <% if (user) { %>
  <div class="alert alert-danger">You are logged in</div>
<% } else { %>
  <div class="alert alert-success">You are not logged in</div>
<% } %>

            <!-- Verification code input + submit button (replaces previous buttons) -->
            <form action="/verifyEmailToken" method="GET" class="mt-3">
              <div class="d-flex justify-content-center">
                <div class="input-group" style="max-width:420px; width:100%;">
                  <input
                    type="text"
                    name="token"
                    class="form-control text-center fs-4"
                    placeholder="Enter 8‑digit code"
                    
                    
                    inputmode="numeric"
                    autocomplete="one-time-code"
                    required
                    aria-label="8 digit verification code"
                  />
                  <input type="hidden" name="email" value="<%= user.email %>" />
                  <button class="btn btn-primary" type="submit">Verify Code</button>
                </div>
              </div>

              <div class="form-text text-center mt-2">
                Didn't receive the code? <a href="/resendVerification">Resend verification email</a>
                &nbsp;·&nbsp;
                <a href="/auth/login">Back to login</a>
              </div>
            </form>

          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>